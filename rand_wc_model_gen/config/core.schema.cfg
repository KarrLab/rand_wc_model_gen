[rand_wc_model_gen]

    [[kb_gen]]
        id = string()
        version = string()
        [[[component]]]
            [[[[ChromosomesGenesGenerator]]]]
                num_chromosomes = integer(min=1)
                avg_gc_frac = float(min=0, max=1)
                avg_num_genes = float(min=1)
                avg_gene_len = float(min=1)
                avg_coding_frac = float(min=0, max=1)

    [[model_gen]]
        id = string()
        version = string()
        [[[component]]]
            [[[[TranscriptionSubmodelGenerator]]]]
